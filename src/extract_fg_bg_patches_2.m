function extract_fg_bg_patches_2(frame_pairs, extObjOpts)
    numTopRegs = extObjOpts.numTopRegs; 
    
    for i=1:length(frame_pairs)
        frame_pair = frame_pairs{i};
        curr_frame = frame_pair.curr_frame;
        next_frame = frame_pair.next_frame;
        
    end
    
end

